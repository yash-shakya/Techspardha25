/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com', 'i.ibb.co', 'res.cloudinary.com', 'github.com'], 
    },
  };
  
  export default nextConfig;
  